
----------------- using express-rate-limiter --------------------

```
npm i express-rate-limit
```

```
import { rateLimit } from 'express-rate-limit'

const limiter = rateLimit({
	windowMs: 15 * 60 * 1000, // 15 minutes
	limit: 100, // Limit each IP to 100 requests per `window` (here, per 15 minutes).
	standardHeaders: 'draft-8', // draft-6: `RateLimit-*` headers; draft-7 & draft-8: combined `RateLimit` header
	legacyHeaders: false, // Disable the `X-RateLimit-*` headers.
	// store: ... , // Redis, Memcached, etc. See below.
})

// Apply the rate limiting middleware to all requests.
app.use(limiter)

```


--------------------- But, still it is vulnerable to DDoS ----------------------------
though, ddos is rarely used for password reset, it is usually used to choke a server. 


---  Why do attackers do DDoS. 
- To charge ransom because the service remains down until DDoS lifted.
- On sneaker drop events/NFT mints where the faster the request reaches server the bettter.


--- How can you save your reset password endpoint.
- You can implement logic that only 3 resets allowed per emails sent out.
- You can implement Captcha logic.


Captchas - to verify the user is human not bot.

There are various free available captchas, cloudflare Turnstile is one of them.


----------------- Adding Captchas via cloudflare --------------------

1.  Add a new site to Turnstile. 
-	Keep your site key and site secret safe.

2.	Create a react project

3.	Add https://github.com/marsidev/react-turnstile
```
npm i @marsidev/react-turnstile
```

4. Update App.tsx
```

```

